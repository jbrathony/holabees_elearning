import React, { Fragment } from 'react';
import Chess from 'react-chess';

class TestComp extends React.Component {
  render() {
    return (
      <Chess style={{ height: '100%' }} />
    );
  }
}


export default TestComp;
